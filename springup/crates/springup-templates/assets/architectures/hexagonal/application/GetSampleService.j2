package {{ package_name }}.application;

import {{ package_name }}.domain.model.Sample;
import {{ package_name }}.domain.port.in.GetSampleUseCase;
import {{ package_name }}.domain.port.out.SampleRepository;

import java.util.Optional;

/**
 * Application-layer implementation of {@link GetSampleUseCase}.
 *
 * Generated by springup {{ springup_version }}.
 */
public class GetSampleService implements GetSampleUseCase {

    private final SampleRepository repository;

    public GetSampleService(SampleRepository repository) {
        this.repository = repository;
    }

    @Override
    public Optional<Sample> findById(Long id) {
        return repository.findById(id);
    }
}
