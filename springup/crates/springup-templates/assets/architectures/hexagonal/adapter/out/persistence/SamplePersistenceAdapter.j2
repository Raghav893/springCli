package {{ package_name }}.adapter.out.persistence;

import {{ package_name }}.domain.model.Sample;
import {{ package_name }}.domain.port.out.SampleRepository;
import org.springframework.stereotype.Component;

import java.util.Optional;

/**
 * Driven adapter: implements the {@link SampleRepository} outbound port using a JPA repository.
 *
 * Translates between the persistence entity ({{ package_name }}.adapter.out.persistence.SampleEntity)
 * and the pure domain model ({{ package_name }}.domain.model.Sample).
 *
 * Generated by springup {{ springup_version }}.
 */
@Component
public class SamplePersistenceAdapter implements SampleRepository {

    private final SampleJpaRepository jpaRepository;

    public SamplePersistenceAdapter(SampleJpaRepository jpaRepository) {
        this.jpaRepository = jpaRepository;
    }

    @Override
    public Optional<Sample> findById(Long id) {
        return jpaRepository.findById(id).map(this::toDomain);
    }

    private Sample toDomain(SampleEntity entity) {
        return new Sample(entity.getId(), entity.getName(), entity.getDescription());
    }
}
