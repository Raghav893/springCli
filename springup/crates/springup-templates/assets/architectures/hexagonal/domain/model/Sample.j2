package {{ package_name }}.domain.model;

/**
 * Sample domain entity (pure domain, no JPA annotations).
 *
 * Generated by springup {{ springup_version }} as part of the hexagonal architecture skeleton.
 */
public record Sample(Long id, String name, String description) {
    public Sample {
        if (name == null || name.isBlank()) {
            throw new IllegalArgumentException("name must not be blank");
        }
    }
}
