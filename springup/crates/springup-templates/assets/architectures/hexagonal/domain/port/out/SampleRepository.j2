package {{ package_name }}.domain.port.out;

import {{ package_name }}.domain.model.Sample;

import java.util.Optional;

/**
 * Outbound port (driven): the application core's abstraction over persistence.
 *
 * A JPA-backed implementation lives in `adapter/out/persistence` and is wired at runtime.
 *
 * Generated by springup {{ springup_version }}.
 */
public interface SampleRepository {
    Optional<Sample> findById(Long id);
}
