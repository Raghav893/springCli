package {{ package_name }}.domain.port.in;

import {{ package_name }}.domain.model.Sample;

import java.util.Optional;

/**
 * Inbound port (use case): retrieve a sample by id.
 *
 * The application core defines this port; adapters in `adapter/in/web` drive it via REST
 * controllers, and adapters in `adapter/out/persistence` satisfy its data needs.
 *
 * Generated by springup {{ springup_version }}.
 */
public interface GetSampleUseCase {
    Optional<Sample> findById(Long id);
}
